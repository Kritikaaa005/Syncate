// LOCATION: syncate-mobile/src/app/dashboard/calendar.tsx
// (replaces the existing file)
//
// === CHANGED: used to render DashboardCalendarScreen (the single-
// date logging picker). That screen moved to log-period.tsx — this
// path is now the year-long color-coded view, since that's what the
// bottom nav's "Calendar" tab should actually open.

export {
  default,
} from "@/screens/dashboard/CycleCalendarScreen";